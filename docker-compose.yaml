
services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"

  frontend:
    build:
      context: .   
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend